<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.itwill.joo.repository.PaymentRepository">
	<insert id="insertPaymentData">
		insert into PAYMENTS (O_ID, PAY_METHOD, IMP_UID, MERCHANT_UID, AMOUNT, PCREATED_TIME)
		values(#{o_id}, #{pay_method}, #{imp_uid}, #{merchant_uid}, #{amount}, systimestamp)
	</insert>
</mapper>